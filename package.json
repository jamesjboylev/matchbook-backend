{
  "name": "matchbook-price-backend",
  "version": "1.0.0",
  "description": "Small proxy backend that fetches real security quotes server-side and serves them to the Matchbook frontend.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
